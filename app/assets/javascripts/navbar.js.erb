$( document ).ready(function() {
   $('.arc').arctext({radius: 70, dir: -1});


    $(window).resize(function(){

        if ($(window).width() <= 767) {


            var path = "<%= image_path('Manazil logo.svg')%>";
            $('.arc').arctext({radius: 70000, dir: -1});
            $("#imigo").attr("src",path);
            console.log('imigo');
        }

        if ($(window).width() > 767) {


            var path = "<%= image_path('Manazil logo 2.svg')%>";
            $('.arc').arctext({radius: 70, dir: -1});
            $("#imigo").attr("src",path);

        }


    });




    if ($(window).width() <= 767) {


        var path = "<%= image_path('Manazil logo.svg')%>";
        $('.arc').arctext({radius: 70000, dir: -1});
        $("#imigo").attr("src",path);
        console.log('imigo');
    }

    if ($(window).width() > 767) {


        var path = "<%= image_path('Manazil logo 2.svg')%>";
        $('.arc').arctext({radius: 70, dir: -1});
        $("#imigo").attr("src",path);

    }




});